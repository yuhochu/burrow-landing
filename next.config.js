/** @type {import('next').NextConfig} */
const nextConfig = {
    compiler: {},
    reactStrictMode: true,
    redirects: async () => [],
    rewrites: async () => [],
};

module.exports = nextConfig;
